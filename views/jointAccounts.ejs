<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=devide-width" />
    <style></style>

    <script type="text/javascript" src="/Project1/models/menu.model.js">
      function goToAnotherPage() {
          const PORT = 3000;
          window.location.href = `http://localhost:${PORT}/incomes`; // Replace with your desired URL
      }
    </script>

    <title>Joint Account Page</title>
    <link rel="stylesheet" href="./css/home.css" />
  </head>
  <body style="background-color: rgb(183, 208, 207)">
    <br />
    <div class="topnav">
      <a class="active" href="/home">Home</a>
      <a href="/newIncomeInfo">Incomes+</a>
      <a href="/newExpenseInfo">Expenses-</a>
      <a href="/currentStatus">CurrentStatus</a>
      <a href="/budgets">Budgets$</a>
      <a href="/transaction">Transfer</a>
      <a href="/myProfile">My Profile</a>
    </div>
    <h1 style="text-align: center">Current Stats</h1>

    <div style="text-align: center">
      <img src="./images/usericon.png" onclick="goToAnotherPage()" />
    </div>

    <h3 style="text-align: center"><%= currentUser.name %></h3>
    <p style="text-align: center">User ID: <%= currentUser.userID %>'s</p>

    <%if(currentJointAccounts.length >= 1){ %>

    <h2 style="text-align: center">Current Budgets</h2>
    <div style="text-align: center" class="table-container">
      <table border="1">
        <th>UserID1</th>
        <th>UserID2</th>
        <th>UserID3</th>
        <th>UserID4</th>
        <th>UserID5</th>
        <th>AccountID</th>
        <th>Amount</th>
        <% for (var i in currentJointAccounts ) { %>
        <tr>
          <td><%= currentJointAccounts[i].param1 %></td>
          <td><%= currentJointAccounts[i].param2 %></td>
          <td><%= currentJointAccounts[i].param3 %></td>
          <td><%= currentJointAccounts[i].param4 %></td>
          <td><%= currentJointAccounts[i].param5 %></td>
          <td><%= currentJointAccounts[i].param6 %></td>
          <td><%= currentJointAccounts[i].param8 %></td>
        </tr>
        <%} %>
      </table>
    </div>

    <%} %> <%if(currentJointAccounts.length < 1){ %>

    <h2 style="text-align: center">No Current Joint Accounts</h2>

    <%} %>

    <div style="text-align: center">
      <form
        action="/createJointAccount"
        method="get"
        style="text-align: center"
      >
        <button type="submit">Create New Joint Account</button>
      </form>
    </div>
    <br />
  </body>
</html>
