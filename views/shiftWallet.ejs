<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=devide-width" />
    <style></style>

    <script
      type="text/javascript"
      src="/Project1/models/menu.model.js"
    ></script>

    <title>Shift Wallet Page</title>
    <link rel="stylesheet" href="./css/home.css" />
  </head>
  <body style="background-color: rgb(183, 208, 207)">
    <br />
    <div class="topnav">
      <a class="active" href="/home">Home</a>
      <a href="/newIncomeInfo">Incomes+</a>
      <a href="/newExpenseInfo">Expenses-</a>
      <a href="/currentStatus">CurrentStatus</a>
      <a href="/budgets">Budgets$</a>
      <a href="/transaction">Transfer</a>
      <a href="/myProfile">My Profile</a>
    </div>
    <h1 style="text-align: center">Current Stats</h1>

    <div style="text-align: center">
      <img src="./images/usericon.png" />
    </div>

    <h3 style="text-align: center"><%= currentUser.name %></h3>
    <p style="text-align: center">User ID: <%= currentUser.userID %>'s</p>

    <%if(currentUser.wallets >= 1){ %>

    <div style="text-align: center">
      <h2>Current Wallets</h2>
      <div style="text-align: center" class="table-container">
        <table border="1">
          <th>Wallet</th>
          <th>Amount</th>

          <% for (var i =1; i <= currentUser.wallets; i++ ) { %>
          <tr>
            <td><%=i%></td>
            <td><%= currentUser.amounts[i]%></td>
          </tr>
          <%} %>
        </table>
      </div>
    </div>
    <br />

    <div style="text-align: center">
      <form action="/shiftWallet" method="post" style="text-align: center">
        <h3 style="text-align: center">
          Select the wallet you want shift money from
        </h3>
        <input
          type="text"
          name="shiftFromWalletID"
          id="shiftFromWalletID"
          placeholder="Enter wallet ID to shift from"
        />

        <h3 style="text-align: center">
          Select the wallet you want shift money to
        </h3>
        <input
          type="text"
          name="shiftToWalletID"
          id="shiftToWalletID"
          placeholder="Enter wallet ID to shift money to"
        />

        <h3 style="text-align: center">Shift amount</h3>
        <input
          type="text"
          name="shiftAmount"
          id="shiftAmount"
          placeholder="Enter shift amount"
        />

        <button type="submit">Shift Money</button>
      </form>
    </div>
    <br />

    <% } %> <%if(currentUser.wallets < 1){ %>

    <h2 style="text-align: center">No Current Wallets</h2>

    <%} %>
  </body>
</html>
